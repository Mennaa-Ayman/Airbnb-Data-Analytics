[project]
name = "AirBnb_Analysis"
version = "0.1.0"
description = "The GUI required for the course project"
authors = [
    {name = "bonniesimmer",email = "noura19918@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "streamlit (>=1.56.0,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
